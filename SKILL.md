---
name: novel-lore-distill
description: 将长篇小说或长文本蒸馏成可追溯、可检索、可继续编辑的设定资料库。用户提到小说蒸馏、世界观资料库、角色卡、人物关系、事件或情节线、地点、势力、组织、物品、能力、规则、术语、时间线、世界书、Lorebook、SillyTavern 角色卡，或想把整本小说整理成结构化设定集时使用。适用于 TXT、EPUB、PDF 转出的文本、字幕和其他长篇资料；即使用户只说“把这本小说做成卡”，也应触发。
---

# 小说设定蒸馏

## 这个 skill 做什么

本 skill 是一套**可追溯的小说设定资料提取流程**，不是读后感生成器，也不是把作者的写作方法论做成 skill。它把长篇小说拆成可检查的章节分块，保留中间提取结果和来源链，再合并为可查询、可维护、可转换的资料库。

默认支持以下通用类别，按原文实际情况取舍：

- 人物/角色卡：身份、别名、关系、关键经历、目标、结局和出场章节
- 事件/情节/任务卡：案件、战役、冒险、比赛、灾难、任务、剧情线或其他关键事件
- 地点/场景卡：地理位置、功能、历史变化、场景规则和关联实体
- 势力/组织/阵营卡：成员、目标、结构、资源、关系和阶段变化
- 物品/资源/技术卡：来源、属性、用途、持有者、限制和变化
- 能力/技能/制度/规则卡：作用对象、触发条件、代价、限制、演变和例证
- 术语与世界观词典：专有名词、制度、文化、历史、技术、自然法则和设定解释
- 大事记/时间线：按章节或原文时间排序的关键事件

题材可以是奇幻、科幻、推理、历史、现实、武侠、言情、冒险或混合题材。不要把某一题材的分类强加给另一题材；只有原文确实存在相应概念时才建立对应卡片。

每条重要事实尽量保留 `source_file`、`source_chunk`、`source_chapter`、`related_entity` 和 `evidence_status`。正式卡片还必须有稳定的 `entity_id`，并在 `证据` 区块逐条记录定位、证据类型、置信度和事实/转述/推断状态。没有来源证据的内容不能写成确定事实，详细格式见 [`references/card-schemas.md`](references/card-schemas.md)。

## 触发后的第一步

先确认或从上下文读取：

1. 原文路径和格式。没有正文文本时，不凭记忆提取设定。
2. 书名、作者和系列信息。未知时标为“未知”，不要猜测。
3. 处理范围：试点章节、单卷、全书，还是只做指定类别。
4. 输出形式：Markdown 资料库，以及是否需要后续转换为 World Book、Lorebook 或角色卡 JSON。
5. 是否允许在源文件旁创建新的输出目录。默认允许创建新目录，但不覆盖原文。

如果用户已经明确要求全书批量处理，不要重复询问范围；仍然先盘点文本规模，说明分块数量和运行成本，然后执行全量流程。默认输出目录为源文件旁的 `<书名>-蒸馏/`，先写入 `PIPELINE_STATE.md`，便于中断后续跑。

## 总体目录

```text
<书名>-蒸馏/
├── 00-总览.md
├── PIPELINE_STATE.md
├── manifest.json             # 原始文件路径、SHA-256、大小和修改时间
├── audit-config.json         # 可选：题材或项目自定义审计配置
├── 角色索引.md
├── 事件索引.md
├── 地点索引.md
├── 势力索引.md
├── 物品索引.md
├── 能力索引.md
├── 术语表.md
├── 大事年表.md
├── 别名映射.md
├── 角色/                    # 一人物一文件
├── 事件/                    # 一事件或情节线一文件
├── 地点/                    # 一地点或场景一文件
├── 势力/
├── 物品/
├── 能力/
├── 分块文本/                # 原文分块和范围记录
├── 分块提取/                # 分块级结构化提取
├── 合并/                    # 跨分块的一级合并
├── 终审输入/                # 提供给二级终审的完整来源集合
└── 修复/                    # 基线、备份、审计报告和暂存产物
```

`分块文本/`、`分块提取/`、`合并/` 和 `修复/` 不是无用临时文件，而是定位遗漏、回滚和断点续跑所需的证据链。合并文件数量由实际处理范围决定，不要为适配某一本书强行写死数量。

## 阶段 0：盘点与切分

1. 检查文件大小、编码、行尾、章节或场景标题、最小和最大章节号；用 `scripts/create_lore_manifest.py` 记录原文路径、SHA-256、大小和修改时间。
2. 章节识别按证据逐步放宽：优先匹配明确的中文章节标题，也可识别“第 X 回”、中文数字章节、大小写不敏感的英文 `Chapter X`、序章和终章等格式；记录误匹配和无法识别的范围。题材或文本使用其他格式时，通过 `config.example.json` 自定义规则。
3. 按章节或稳定场景边界切块。默认每块约 15-25 章，或约 1-3 万汉字；实际大小以完整上下文和模型限制为准。超长章节可以单独拆分，但不能让代理只读到半个逻辑段落。
4. 第一块保留书名、作者、卷名和简介等元数据；最后一块保留完结标记、后记或尾声信息。
5. 用脚本检查分块数量、编号连续性、首尾覆盖范围、分块总字节数与原文是否相符。CRLF 变成 LF 不算内容丢失，但要记录。

如果用户只要求部分章节，仍然记录实际范围，并在 `PIPELINE_STATE.md` 明确说明这不是全书覆盖。

## 阶段 1：分块提取

每个子代理只负责一个分块，必须完整读取该分块，写入自己的 `分块提取/chunk-NNN.md`。禁止两个代理写同一个文件。默认使用以下八类栏目，原文没有对应内容时保留标题并写“本块未发现”；实际项目可通过配置替换或缩减分类：

1. 人物/角色出现
2. 事件/情节/任务
3. 地点/场景
4. 势力/组织/阵营
5. 物品/资源/技术
6. 能力/技能/制度/规则
7. 术语与世界观
8. 大事记/时间线

每条记录必须带章节、场景或可定位的原文范围；事实和推断分开；直接引文尽量不超过 30 字，并优先用转述说明事实。无名对象可以留在大事记或事件参与者中，不必强行建立人物卡。

正式卡片必须保留稳定 `实体 ID`。每条证据使用唯一 `evidence_id`，并记录 `source_file`、`source_chunk`、`source_chapter` 或其他定位、`evidence_type`、`confidence` 和 `fact_status`；缺少其中必要信息时标记待复核，不得用“根据原文”代替定位。

### 并发调度

并发上限不能写死，采用自适应策略：

- 首轮使用 3-4 个代理；全部成功后再逐步增加；
- 出现 `concurrency limit`、`quota`、HTTP/2 或模型请求错误时，退回上一次成功并发，只重跑失败编号；
- 大任务按实体分片或类别分片，避免一个代理一次生成数百个文件；
- 每个代理使用独立暂存目录；终审前不覆盖正式卡片目录；
- 代理最终回复只报告统计，详细内容写入文件，减少主上下文污染。

## 阶段 1.5：一级合并

把连续分块按范围合并为 `合并/merge-NN.md`。合并代理必须：

- 完整读取所负责范围内的全部分块提取文件；
- 合并跨分块重复实体，章节范围取并集；
- 后期证据可以修正早期判断，但保留早期说法和冲突说明；
- 保留八个一级栏目，即使某一栏为空也保留标题；
- 不把同名自动视为同一对象，也不把不同题材概念强行映射成同一类型；
- 同时识别 `#`、`##`、`###` 标题层级，避免因层级不一致漏入终审输入。

合并文件的编号应从 01 连续到实际最大编号。不要假设所有小说都需要固定数量的合并文件。

## 阶段 1.6：终审输入覆盖校验

启动二级终审前，为每个需要终审的类别生成一份终审输入。默认类别为：角色、事件、地点、势力、物品、能力、术语、大事记；用户只指定部分类别时可以缩小范围。每份终审输入都必须列出 `合并/` 中当前实际存在的全部 `merge-NN.md` 来源，补充文件也要去重后纳入。

自动检查应报告：

```text
终审输入文件数 = 实际终审类别数
每份文件的主来源数 = 合并目录中可用 merge 文件总数
每份文件都包含全部实际 merge 编号 = 是/否
```

不要把某几个固定编号或固定数量的文件当成通用规则。来源集合随文本规模和切分策略变化。

## 阶段 2：二级终审与卡片重建

不要让一个代理负责全书所有实体。按类别或实体名单分片，全部写入 `修复/暂存/`；每片完成后再装配正式目录。正式卡片以证据为准，不用类型模板补造原文没有的信息。

### 角色卡最低字段

```markdown
# 姓名或原文称呼
## 实体状态
核心 / 次要 / 仅提及 / 待复核
## 别名与称呼
## 身份与背景
## 目标、动机与性格
## 能力、技能与资源
## 关键经历（按章节）
## 人物关系
## 最终状态或结局
## 出场章节
## 来源
```

### 事件卡最低字段

事件可以是案件、战役、任务、比赛、冒险、灾难、冲突、剧情线或其他对文本有影响的事件。

```markdown
# 事件名
## 实体状态
核心 / 次要 / 仅提及 / 待复核
## 事件类型与时间
## 地点或涉及场景
## 起因、目标与触发条件
## 参与者与关联势力
## 关键经过
## 规则、限制或关键线索
## 结果、影响与后续
## 章节范围
## 来源
```

### 地点卡最低字段

```markdown
# 地点名
## 实体状态
核心 / 次要 / 仅提及 / 待复核
## 地点类型与位置
## 功能、环境与重要场景
## 历史变化
## 关联人物、事件与势力
## 出现章节
## 来源
```

### 势力卡最低字段

```markdown
# 势力名
## 实体状态
核心 / 次要 / 仅提及 / 待复核
## 类型、目标与立场
## 组织结构与成员
## 资源、能力与行动方式
## 与其他实体的关系
## 阶段变化与最终状态
## 出现章节
## 来源
```

### 物品卡最低字段

```markdown
# 物品或资源名
## 实体状态
核心 / 次要 / 仅提及 / 待复核
## 类型、外观或定义
## 来源、持有者与流转
## 功能、用途与影响
## 使用条件、限制与代价
## 变化、损毁或最终状态
## 出现章节
## 来源
```

### 能力/规则卡最低字段

```markdown
# 能力、技能、制度或规则名
## 实体状态
核心 / 次要 / 仅提及 / 待复核
## 适用对象与来源
## 作用与表现
## 触发条件、限制、代价或例外
## 发展、变化与失效条件
## 相关人物、物品、事件或制度
## 例证章节
## 来源
```

### “仅提及”处理

资料极少的对象不要删除，也不要伪装成完整传记。标注“仅提及”，保留唯一可核查的信息和章节/来源。空白字段可以写“原文未说明”，不要用“输入未提供”制造模板噪声。若类别只在某类小说中出现，应在索引中注明“本书未发现”，而不是生成空卡。

## 阶段 2.5：同名、别名与冲突裁决

装配前检查不同分片是否出现：

- 同一对象的别名、译名、称号或阶段性称呼；
- 同名但不同对象；
- 继承、转世、化名、身份揭示等容易误合并的情况；
- 来源之间的身份、时间、能力、关系或结局冲突。

同一对象合并全部经历、关系和结局；同名不同对象在文件名中加入最小必要的区分说明；证据不足时保留多个候选并标记“待复核”。冲突记录写入 `修复/暂存/冲突-NN/`，正式卡片保留裁决依据，不凭名字强行合并。

## 阶段 3：安全装配与索引

1. 覆盖正式目录前，先复制到 `修复/正式目录备份-时间戳/`。
2. 只从已完成的暂存卡片装配正式目录；失败分片不能进入正式目录。
3. 检测 Windows 非法文件名和同名冲突，使用安全替换或编码，但不能改变卡片内的原始实体名。
4. 根索引链接到 `角色/文件.md`、`事件/文件.md`、`地点/文件.md` 等分类目录；目录内索引使用同目录相对路径。两类索引都要检查。
5. 生成 `别名映射.md`，记录明确别名、译名、称号和疑似重名；不能仅凭规范化名字自动合并。

## 阶段 4：全量审计

使用 `scripts/audit_lore_distill.py` 或等价脚本，检查：

```powershell
python scripts/audit_lore_distill.py "D:\小说\某书-蒸馏" --config "D:\小说\某书-蒸馏\audit-config.json" --strict
```

- 分块文本和分块提取编号是否连续，并与实际预期范围一致；
- `合并/` 中的 merge 编号是否连续，每份是否包含八个一级栏目；
- 每份终审输入是否覆盖当前实际存在的全部 merge 来源；
- 时间线章节或原文时间范围是否覆盖已声明的处理范围；未知首尾不要擅自判定为错误；
- 正式卡片目录是否有空文件、缺少实体状态、实体 ID、逐条证据或来源定位；
- 根索引和分类目录索引中的所有 Markdown 链接、锚点是否存在，以及是否有卡片未被索引；
- `manifest.json` 是否可解析，源文件可访问时 SHA-256、大小是否匹配；`PIPELINE_STATE.md` 是否保留固定状态字段；
- 仅提及、待复核、短卡、占位字段和规范化重名数量；
- 与修复前基线相比，新增来源是否进入最终卡片。

审计报告必须区分：

- **确定遗漏**：可由文件或来源计数直接证明；
- **疑似遗漏**：实体可能在其他分块重复出现，需要进一步核对；
- **原文矛盾**：不是提取遗漏，保留多种说法和证据。

## 阶段 5：交付与可选格式

默认交付 Markdown 资料库。确认 Markdown 卡片、索引和严格审计稳定后，才生成 SillyTavern World Book、Lorebook 或角色卡 JSON。JSON 条目的 `key`、`content`、`comment` 和来源应从已审计卡片生成，不要直接从半成品分块提取生成。

最后更新 `PIPELINE_STATE.md`，写清当前阶段、完成产物、分块/合并数量、并发峰值、来源覆盖、备份位置和仍需人工复核的冲突。不能把“有候选记录”写成“全部完成”。

## 边界

- 不凭记忆补全小说设定；没有原文就停止并索要文本来源。
- 不把章节、场景或卷次编号擅自改成“正确编号”；保留原始编号和待复核说明。
- 不把副本、案件、战役、任务或剧情线自动视为同一种事件，除非原文结构和用户需求支持这种归类。
- 不把某一题材的专用概念套用到其他题材；源文使用的专有名词优先原样保留。
- 不删除仅提及对象，只做状态分层并保留证据边界。
- 不覆盖用户原文或未备份的正式资料库。
- 不把候选提取结果冒充已审计定稿。
- 本 skill 不负责作者思想、方法论或人物角色扮演；那属于其他 skill。

## 参考文件

- [`references/card-schemas.md`](references/card-schemas.md)：卡片与中间文件字段规范
- [`references/config-schema.md`](references/config-schema.md)：自定义分类、字段、章节规则和输出路径
- [`references/pipeline-state-schema.md`](references/pipeline-state-schema.md)：`PIPELINE_STATE.md` 固定字段
- [`references/repair-checklist.md`](references/repair-checklist.md)：修复、覆盖率和交付检查清单
- [`references/test-prompts.md`](references/test-prompts.md)：触发和修复分支测试提示
- [`scripts/audit_lore_distill.py`](scripts/audit_lore_distill.py)：最终审计脚本
- [`scripts/create_lore_manifest.py`](scripts/create_lore_manifest.py)：生成原始文件 manifest
