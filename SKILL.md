---
name: novel-lore-distill
description: 将长篇小说或长文本蒸馏成可追溯的设定资料库。用户提到小说蒸馏、角色身份卡、人物卡、副本/闹鬼事件、鬼与能力、势力、道具、术语、大事年表、世界书、Lorebook、SillyTavern角色卡，或想把整本小说整理成可检索设定集时使用。适用于 TXT、EPUB、PDF 转出的文本和长篇资料；即使用户只说“把这本小说做成卡”，也应触发。
---

# 小说设定蒸馏

## 这个 skill 做什么

本 skill 是**设定资料提取流水线**，不是把书的方法论做成 skill。它把长篇小说拆成可追溯的：

- 角色身份卡
- 副本、闹鬼事件、鬼域或任务卡
- 鬼、异能和灵异规则卡
- 势力、组织和团队卡
- 重要道具卡
- 术语与世界观词典
- 按章节排列的大事年表
- 可选的 SillyTavern World Book / Lorebook / 角色卡 JSON

每条重要事实都尽量保留 `source_file`、`source_chunk`、`source_chapter`。没有来源证据的内容不能写成确定事实。

## 触发后的第一步

先确认或从上下文读取：

1. 原文路径和格式；没有正文文本时，不凭记忆提取。
2. 书名、作者；未知时标为未知，不自行猜测。
3. 范围：先试点一个卷/一百章，还是全书批量。
4. 输出：Markdown 资料库，是否还要 SillyTavern JSON。
5. 是否允许在源文件旁创建新的输出目录；绝不直接覆盖原文。

如果用户已经明确说“全书批量”“全套”“直接做”，不要重复询问已确定的选项；仍然创建新输出目录和可回滚备份。

默认输出目录为源文件旁的 `<书名>-蒸馏/`，而不是覆盖源 TXT。先写 `PIPELINE_STATE.md`，方便中断后续跑。

## 总体目录

```text
<书名>-蒸馏/
├── 00-总览.md
├── PIPELINE_STATE.md
├── 角色索引.md          # 根索引，链接到角色/
├── 副本索引.md
├── 鬼索引.md
├── 势力索引.md
├── 道具索引.md
├── 术语表.md
├── 大事年表.md
├── 别名映射.md
├── 角色/                # 一角色一文件
├── 副本/                # 一事件一文件
├── 鬼/
├── 势力/
├── 道具/
├── 分块文本/            # 中间审计产物
├── 分块提取/
├── 合并/
├── 终审输入/
└── 修复/                # 基线、备份、审计报告、暂存输出
```

保留中间文件。它们不是垃圾，而是定位漏项和重跑某一阶段的证据链。

## 阶段 0：盘点与切分

1. 检查文件大小、编码、行尾、章节标题和最后章节号；记录文件 SHA-256。
2. 章节正则要逐步放宽：优先匹配中文数字章节标题，失败时用宽松的行首章节模式，并抽查误匹配。不要假设阿拉伯数字章节一定存在。
3. 按章节边界切块，默认每块 15–25 章；中文长篇通常每块约 5–8 万字。超长块宁可再拆，不要让代理只读半块。
4. 第一块保留书名、作者、卷名和简介；最后一块保留完本标记。
5. 用脚本核对：分块数量、编号是否连续、首尾章节是否覆盖、分块总字节与源文件是否相符。行尾从 CRLF 变成 LF 不算内容丢失，但要记录。

## 阶段 1：分块提取

每个子代理只负责一个分块，必须完整读取该块（超过 2000 行时用 `Read(offset, limit)` 分段），写到自己的 `分块提取/chunk-NNN.md`。禁止两个代理写同一文件。

统一提取七类：

1. 角色出现
2. 副本/闹鬼事件/鬼域
3. 鬼与灵异实体
4. 势力/组织
5. 重要道具
6. 术语与设定
7. 大事记

每个条目必须带章节号；只依据原文；直接引文尽量不超过 30 字，以转述为主。无名路人可放入大事记，不必每人建卡。

### 并发调度

并发上限不是固定值。采用自适应策略：

- 首轮试 3–4 个代理；全部成功后再试 5–6 个；
- 出现 `concurrency limit`、`quota`、HTTP/2 或模型请求错误时，立即退回上一次成功并发；只重跑失败编号；
- 大任务拆成实体分片或类别分片，避免一个代理一次生成数百个文件；
- 每个代理使用独立暂存目录；终审前不覆盖正式卡片目录；
- 代理最终回复只报统计，详细内容写文件，减少主上下文污染。

## 阶段 1.5：一级合并

把若干连续分块合并为 `合并/merge-NN.md`。合并代理必须：

- 逐个完整读取所属分块提取文件；
- 同一实体跨分块合并，章节范围取并集；
- 冲突以章节靠后者为参考，但保留早期说法和冲突说明；
- 保留七个大节，即使某节为空也写标题；
- 不把“同名”自动当成“同一人”。

**标题层级不能写死。** 解析一级合并时同时识别 `#`、`##`、`###` 三种标题层级。曾经发生过某些 merge 文件使用 `##`、其他文件使用 `###`，导致整个 merge 被漏出终审输入。

## 阶段 1.6：终审输入覆盖校验

在启动二级终审前，生成七份终审输入：

- 角色
- 副本
- 鬼
- 势力
- 道具
- 术语
- 大事记

每一份都必须明确列出 `merge-01.md` 到 `merge-09.md` 九个来源。自动检查以下断言：

```text
终审输入文件数 = 7
每个文件的主来源数 = 9
每个文件都包含 merge-04.md 和 merge-08.md
```

若某个 `merge-08-补充` 文件来自中断恢复，且完整 `merge-08.md` 已经包含相同栏目：补充文件只留作审计，不要和主文件重复输入。若主文件确实缺节，先把补充内容去重后合入，再进行终审。

## 阶段 2：二级终审与卡片重建

不要让一个代理负责全书所有角色。按类别或实体名单分片：

- 角色：按实体名单分 3–6 片；
- 鬼：按实体名单分 3–6 片；
- 副本、势力、道具：通常一类一代理或两片；
- 术语和年表独立处理。

全部写入 `修复/暂存/`。确认各片完成后再装配正式目录。

### 角色卡最低字段

```markdown
# 姓名
## 实体状态
核心 / 次要 / 仅提及 / 待复核
## 别名
## 身份
## 所属势力
## 能力与装备
## 性格与行为
## 关键经历（按章节）
## 人物关系
## 最终状态/结局
## 出场章节
## 来源
```

### 副本卡最低字段

地点、起因背景、触发/进入条件、涉及的鬼及规则、死亡条件与禁忌、逃生/破解方法、死亡名单、幸存者、涉及角色、章节范围、阶段结局、来源。

### 鬼卡最低字段

实体状态、别名/特征名、等级或评价、能力与灵异规则、杀人/触发条件、克制/镇压/收封方式、出现章节、演变与最终状态、来源。

### 势力/道具卡

都必须有实体状态、身份/性质、成员或归属、能力/效果、变化或流转、章节范围、来源。

### “仅提及”处理

资料极少的对象不要删除，也不要伪装成完整身份卡。标为“仅提及”，并保留唯一可核查信息和章节/来源。不要用“输入未提供”填充空字段；空字段可写“原文未说明”，但应尽量只保留有意义的字段。

## 阶段 2.5：同名冲突裁决

装配前检查不同分片是否出现同名文件：

- 同一人物：合并全部经历、关系、结局；
- 同名不同人：文件名加区分说明，例如 `赵丽（四楼信使）.md` 和 `赵丽（树林逃生信使）.md`；
- 证据不足：保留两张或标记“待复核”，不要凭名字强合；
- 冲突裁决文件单独写入 `修复/暂存/角色-冲突-NN/`，正式装配时排除冲突前原卡，加入裁决卡。

## 阶段 3：安全装配与索引

1. 覆盖正式目录前，先复制到 `修复/正式目录备份-时间戳/`。
2. 只从已完成暂存片装配正式目录；失败代理的残片不进入正式目录。
3. Windows 非法文件名字符要替换或安全编码；检测文件名冲突，不要静默覆盖。
4. 根索引从根目录链接到 `角色/文件.md`、`副本/文件.md` 等；目录内索引使用同目录相对文件名。两类索引都要检查。
5. 生成 `别名映射.md`：记录明确别名和去掉括号说明后的疑似重名，但不要仅凭规范化名字自动合并。

## 阶段 4：全量审计

使用 `scripts/audit_lore_distill.py`，或等价脚本，检查：

- `分块文本`和`分块提取`是否编号连续；
- `merge-01..09` 是否全部存在且每份七节齐全；
- 7 份终审输入是否都是 9/9 来源；
- 章节最小值/最大值是否覆盖源文首尾（例如第1章到最后一章）；
- 正式卡片目录无空文件；
- 所有卡片有实体状态和来源字段；
- 根索引、目录索引坏链为 0；
- 统计仅提及、待复核、短卡、占位字段和规范化重名；
- 对比修复前基线，确认新增来源确实进入最终卡片。

审计报告必须区分：

- **确定遗漏**：可由文件/来源计数直接证明；
- **疑似遗漏**：实体可能在其他块重复出现，需进一步核对；
- **原文矛盾**：不是提取遗漏，保留多种说法。

## 阶段 5：交付与可选格式

默认交付 Markdown。确认 Markdown 资料库稳定后，才生成 SillyTavern World Book、Lorebook 或 V2 角色卡 JSON；JSON 条目的 `key`、`content`、`comment` 和章节来源应从已审计卡片生成，不要直接从半成品分块提取生成。

最后更新 `PIPELINE_STATE.md`，写清：当前阶段、完成产物、并发峰值、来源覆盖、备份位置、仍需人工复核的冲突。不能把“有候选记录”写成“全部完成”。

## 边界

- 不凭记忆补全小说设定。
- 不把章节错号擅自改成“正确编号”；保留原始编号和待复核说明。
- 不删除仅提及角色、疑似鬼或传闻副本，只做状态分层。
- 不覆盖用户原文或未备份的正式资料库。
- 本 skill 不负责作者思想、方法论或人物角色扮演；那属于其他 skill。
